pub struct RBuilder {
    pub num_players: usize,
    pub num_max_input_bytes: usize,
}

impl RBuilder {
    pub fn new(num_players: usize, num_max_input_bytes: usize) -> Self {
        unimplemented!();
    }
}

//TODO: revisit with an actual builder class?
